name: Check Repository

on:
  push:
    branches: [main]

jobs:
  test-build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Repository checked
        run: echo "Kapanin repository checked successfully."